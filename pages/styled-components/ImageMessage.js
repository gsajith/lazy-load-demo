import styled from 'styled-components';

const ImageMessage = styled.div`
  font-weight: 700;
  opacity: 0.4;
  margin-top: 8px;
  max-width: 150px;
  text-align: center;
`;

export default ImageMessage;
